<?xml version="1.0" encoding="utf-8"?>
<!--
  Reusable scrollable text dialog layout for the EasyTier plugin.
  Used by the log viewer and network info dialogs.

  Key improvements over the previous bare-TextView approach:
  - ScrollView wrapper for smooth vertical scrolling
  - textIsSelectable="true" for copy/select
  - No setHorizontallyScrolling — long lines wrap automatically
  - Max height is set programmatically in the Activity to ~60% of screen
    height, so the dialog doesn't overflow on large logs/network info.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scroll_container"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:fillViewport="true"
    android:scrollbars="vertical">

    <TextView
        android:id="@+id/tv_dialog_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="monospace"
        android:textIsSelectable="true"
        android:textSize="11sp"
        android:lineSpacingExtra="2dp"
        android:padding="16dp" />

</ScrollView>
